import file = CKEDITOR.ui.dialog.file;

export interface Jobregistration {
   file: file;
   birthDay: Date;
   numberYearsExperience: number;
   academicLevel: string
   desiredSalary: string
   desiredWorkingAdress: string
   desiredWorkingForm: string
}
